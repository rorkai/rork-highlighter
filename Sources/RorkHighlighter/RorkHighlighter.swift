/// Provides package metadata shared by applications and diagnostic tools.
///
/// The namespace has no instances. Create a ``Highlighter`` to highlight text
/// or to open an incremental ``HighlightSession``.
public enum RorkHighlighter {
    /// Identifies the source-compatible package version.
    public static let version = "0.2.0"
}
